import os, subprocess, json

# Update tools/build_composer.py with the balanced slot configuration
with open('tools/build_composer.py', 'r', encoding='utf-8') as f:
    code = f.read()

# Let's inspect the getLayoutSlots function in tools/build_composer.py
# and update it with harmonious typographic proportions.
